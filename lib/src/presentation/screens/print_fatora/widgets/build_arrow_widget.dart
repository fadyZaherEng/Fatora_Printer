import 'package:flutter/material.dart';

class BuildArrowWidget extends StatelessWidget {
  const BuildArrowWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
